import fs from "fs";

// Regenerates admin.html track-editor fragment from git history if needed.
// The canonical admin page is admin.html (leaderboard + track editor).
console.log("admin.html is maintained directly. Use scripts/fix-index-tracks.mjs for index.html track extraction.");
